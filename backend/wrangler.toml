name = "brickbit-api"
main = "worker.js"
compatibility_date = "2026-07-01"

# Orígenes permitidos para CORS, separados por comas.
# En producción cámbialo por tu dominio, p. ej. "https://brickbit.co"
[vars]
ALLOWED_ORIGINS = "*"

# Para habilitar los enlaces cortos de "Compartir proyecto":
#   1. npx wrangler kv namespace create SHARES
#   2. Copia el id que imprime y descomenta estas líneas:
#
# [[kv_namespaces]]
# binding = "SHARES"
# id = "PEGA_AQUI_EL_ID"

# Alertas Valor Futuro: cron mensual (día 1, 14:00 UTC). Solo actúa si
# existen el KV SHARES y el secreto RESEND_API_KEY; si no, no hace nada.
[triggers]
crons = ["0 14 1 * *"]
