# BrickBit Atlas — dependencias
# Separadas de requirements.txt de la raíz (Streamlit Cloud / Morfogénesis) a
# propósito: el Atlas pide un stack geoespacial mucho más pesado y no tiene por
# qué arrastrarlo al Motor de Morfogénesis.

# --- Fase 0: ingesta y lago (lo único que hace falta hoy) ---
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pandas>=2.2
numpy>=1.26
pyarrow>=15
h3>=4.0
requests>=2.31
PyYAML>=6.0
pytest>=8.0

# --- Fase 1: estructura espacial ---
libpysal>=4.9        # matrices de pesos W
esda>=2.5            # I de Moran, LISA
scipy>=1.11
scikit-learn>=1.4

# --- Fase 2: modelado e incertidumbre ---
spreg>=1.4           # Durbin espacial (SDM) por máxima verosimilitud
statsmodels>=0.14    # hedónico OLS con errores robustos HC1
shap>=0.45           # explicabilidad; sin él se usa importancia por permutación
# El boosting de media y de cuantiles sale de scikit-learn
# (HistGradientBoostingRegressor con loss='quantile'): maneja NaN de forma
# nativa y evita dos dependencias más. El conformal (CQR + Mondrian) está
# escrito en `atlas/modelos/conforme.py`, unas 60 líneas: mapie y crepes
# traerían más superficie de la que ahorran, y aquí la garantía tiene que
# poder leerse y auditarse.
# mgwr>=2.2          # coeficientes locales — pendiente, ver README

# --- Fase 3: superficies ---
# pykrige>=1.7       # kriging ordinario
# rasterio>=1.3      # GeoTIFF

# --- Fase 4: app ---
# streamlit>=1.35
# pydeck>=0.9
